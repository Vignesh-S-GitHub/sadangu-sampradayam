services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sadangu
      POSTGRES_USER: sadangu
      POSTGRES_PASSWORD: sadangu
    ports:
      - "5432:5432"
    volumes:
      - sadangu_pg:/var/lib/postgresql/data

volumes:
  sadangu_pg:
